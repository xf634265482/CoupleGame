---
description: PVE 命运远征模块的当前玩法、编码与文档同步规则
globs: assets/scripts/pve/**/*.ts,cloudfunctions/pve/**/*.js,cloudfunctions/common/pve/**/*.js,specs/260608-pve-destiny-expedition/**/*.md
alwaysApply: false
---

# PVE 模块规则

PVE 与其他玩法相互独立。PVE 规则以 `specs/260608-pve-destiny-expedition/design.md` 为唯一主文档。

## 玩法与代码

- 修改楼层、战斗、怪物、装备、命痕、职业、体力或奖励时，同步更新主设计文档和验收项。
- `assets/scripts/pve/core/` 保持零 Cocos 依赖，不得直接 `import 'cc'`。
- 随机数统一使用 `core/rng.ts`，不得直接调用 `Math.random()`。
- 当前远征是永久逐层挑战：第一至十四层使用现有链路，第三至第五章代码保持隔离，章节整体完成前不调整当前战斗基准。
- 装备只使用当前固定 85 件目录；怪物使用当前章节目录。
- 每次楼层挑战消耗 5 点体力，首次教程挑战免费；体力上限 60，每 5 分钟恢复 1 点。

## 云端

- `cloudfunctions/common/` 是共享逻辑唯一源；修改后运行 `node scripts/sync-cloud-common.js`。
- 云端保存永久档案、挑战状态与结算结果，并校验层数连续性、奖励边界和请求幂等性。
- 客户端不得直接编辑永久资源或绕过挑战生命周期。

## 资源与发布

- 真机首屏资源按 `cocos-wechatgame-subpackage.mdc` 的主包规则处理。
- 重大修改后执行游戏类型检查、PVE 测试、云端测试和微信构建配置检查。
